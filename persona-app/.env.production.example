# PersonaLayer frontend production env.
# Base URL of the deployed backend API.
VITE_PERSONALAYER_API_BASE=https://personalayer.onrender.com

# Require a connected session before showing /app (1/true to require).
VITE_PERSONALAYER_REQUIRE_SESSION=1
